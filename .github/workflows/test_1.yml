name: Hola mundo

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
     - name: Hola Mundo
       run: echo "Hola Mundo"

  other:
    runs-on: ubuntu-latest

    steps:
     - name: LS
       run: ls -al